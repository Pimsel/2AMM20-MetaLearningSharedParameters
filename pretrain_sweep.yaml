# Program to run
program: pretrain.py

# Search method (grid, random, bayes)
method: grid

# Project
project: SPO_pretrainer_sweep
entity: 

# Metric to optimize
metric: 
  name: avg_loss
  goal: minimize

# Parameter spaces to search
parameters:
  omega_0:
    distribution: categorical
    values: [30, 45, 60]