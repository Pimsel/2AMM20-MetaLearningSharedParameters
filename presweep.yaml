# Program to run
program: pretrain.py

# Search method (grid, random, bayes)
method: random

# Project
project: SPO_pretrainer_SnelliusSweep2
entity: pimdewildt11-eindhoven-university-of-technology

# Metric to optimize
metric: 
  name: avg_loss
  goal: minimize

# Parameter spaces to search
parameters:
  omega_0:
    distribution: categorical
    values: [30, 45, 60]